export enum Status {
  LOADING = "loading",
  SUCCESS = "success",
  ERROR = "error",
}

export enum Theme {
  DARK = "dark",
  LIGHT = "light",
}

export enum Language {
  EN = "en",
  RU = "ru",
  UA = "ua",
}
